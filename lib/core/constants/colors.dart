import 'package:flutter/material.dart';

class AppColors {
  // Streetwear Minimalist Palette (Based on reference image)
  static const Color background = Color(0xFFFFFFFF); // Pure White
  static const Color surface = Color(0xFFFFFFFF);
  
  // Accents
  static const Color primary = Color(0xFF000000); // Pure Black
  static const Color primaryDark = Color(0xFF1A1A1A);
  static const Color accent = Color(0xFFFF5A00); // Orange Accent from reference
  
  // Text
  static const Color textPrimary = Color(0xFF000000);
  static const Color textSecondary = Color(0xFF666666);
  
  // Semantic
  static const Color border = Color(0xFF000000); // Black outlines for pills
  static const Color error = Color(0xFFD32F2F);
  static const Color success = Color(0xFF2E7D32);
}
